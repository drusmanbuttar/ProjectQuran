{
  "name": "projectquran",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Source-linked Quran research web app and installable PWA",
  "scripts": {
    "check": "node --check dist/app.js && node --check dist/catalog.js && node --check dist/sw.js && python3 scripts/validate.py",
    "android:sync": "cap sync android",
    "test:behavior": "node scripts/check-behavior.mjs",
    "ios:sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  },
  "packageManager": "pnpm@11.19.0"
}
